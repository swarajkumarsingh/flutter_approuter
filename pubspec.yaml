name: flutter_approuter
description: Navigation made easy, now navigate to screens, without BuildContext from pure flutter & dart code.
version: 3.0.2
homepage: https://github.com/swarajkumarsingh/flutter_approuter

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
